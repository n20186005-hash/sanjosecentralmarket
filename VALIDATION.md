# Estado de validación

- Auditoría estática de fuente (`scripts/audit-source.mjs`): OK.
- Auditoría de idioma visible (`scripts/audit-language.mjs`): OK (español de Costa Rica, sin restos del template original).
- Logo y favicons locales: presentes en `public/icons/`.
- Páginas independientes: privacidad, términos, cookies, créditos fotográficos y 404: presentes.
- Configuración Astro/Cloudflare: presente (`astro.config.mjs`, `wrangler.jsonc`).
- `site` centralizado y sitemap condicionado a dominio real: presente (`SITE` vacío ⇒ `@astrojs/sitemap` desactivado y etiquetas absolutas omitidas, según lo documentado en README).
- Fotografías reales: 6 archivos `.jpg` copiados en `public/images/` (binarios verificados; autorías y licencias en `PHOTO-SOURCES.md` y en la página de créditos fotográficos).
- Lockfile: regenerado y sincronizado con `package.json` (astro 7.2.10, @astrojs/cloudflare 14.2.6); `pnpm install --frozen-lockfile` responde "Lockfile is up to date".
- Build scripts aprobados para pnpm 11: `pnpm-workspace.yaml` con `allowBuilds` para `esbuild` y `workerd`; elimina `ERR_PNPM_IGNORED_BUILDS` (pnpm 11 sale con código no nulo si no hay configuración). Verificado en local con pnpm 11.24.0 (misma versión que el CI): exit 0, postinstall de esbuild/workerd ejecutado.
- `scripts/verify-build.mjs`: creado y conectado a `pnpm build` / `pnpm deploy`.
- Verificación limpia certificada en este entorno: `astro build` exit 0 y `scripts/verify-build.mjs` exit 0 sobre `dist/` (6 HTML revisados, imágenes locales copiadas, contenido prohibido ausente). La comprobación de sitemap/lastmod se omite correctamente mientras `SITE` esté vacío.
- Pendiente: definir el dominio real en `SITE_URL` (`astro.config.mjs`) y reconstruir para activar sitemap y `lastmod`. `pnpm check` no se ejecutó en este entorno por límite de ejecución de comandos largos. → [SUPERADA 2026-09-02] `SITE` ya está definido como `https://sanjosecentralmarket.com`; sitemap activo (5 URL). `pnpm check` sigue sin ejecutarse localmente.
- Módulo de clima (2026-09-02): nuevo componente `src/components/WeatherSection.astro` (sección `#tiempo`, entre `#visita` y `#comer`) con tiempo actual y pronóstico de 7 días vía Open-Meteo (API gratuita, sin clave, CORS abierto). Arquitectura: proyecto Astro 100 % estático (wrangler sirve `dist/` como assets, sin adapter/SSR), por lo que el fetch se realiza en el navegador con `<script is:inline>` (coordenadas literales 9.934543490167613 / -84.08457912302394, timezone America/Costa_Rica) — NO se usó `define:vars` (en Astro solo opera con `is:inline`; en script normal no inyecta variables). Caché: sessionStorage 10 min (`mc-sj-weather-v1`) + AbortSignal.timeout(12s). Estados: carga / error con reintento / datos; descripciones y emojis por WMO weather_code; enlaces a Open-Meteo e IMN (imn.ac.cr) como pronóstico oficial. CSS añadido en global.css (`.weather-grid/.wt-*/..fc-row/.fc-day/.wt-retry`, responsive 4→2 columnas). Clase `.svc-note` reutilizada. read_lints 0 errores; `astro build` + verify-build OK (exit 0, 6 HTML). Falta comprobar visualmente en navegador el flujo del script (los datos requieren red del cliente).
- Ronda de contenido (2026-09-02): solo adiciones, sin eliminar secciones previas. ① Nueva sección `#servicios` «Lo que puede necesitar alrededor del mercado»: baños, estacionamiento, comida, alojamiento, compras de conveniencia y combustible/carga — descrita por tipos de servicio, sin nombres de comercios, con nota editorial sin fines de lucro. ② Nueva sección `#historias` «Historias y saberes que habitan los pasillos»: 6 tarjetas con etiqueta `.kind` (soda y casado, pasillo de hierbas, sorbetera, terremoto de 1888, del campo a la ciudad, puestos de generaciones) ancladas a fuentes oficiales u observación general. ③ `#historia` ampliada con un párrafo sobre el mercado como «puente» campo-ciudad. ④ FAQ de 6 a 8 preguntas (baños; comida y alojamiento cerca); el FAQPage JSON-LD se actualiza solo desde `faqItems`. ⑤ Navegación ampliada a 7 anclas (se agregan Tradiciones y Servicios). ⑥ CSS: añadida clase `.kind` y `.svc-note` (sin tocar estilos existentes). read_lints: 0 errores.
- Ronda SEO dominio real + PWA (2026-09-02): ① `astro.config.mjs` SITE='https://sanjosecentralmarket.com' (activa canonical absoluto, og:image/og:image:alt, `@astrojs/sitemap` → sitemap-index.xml 5 URL; robots.txt añade `Sitemap:`); ② BaseLayout añade JSON-LD `@graph` WebSite(#website)+Organization(#organization, logo) y WebPage(#webpage, dateModified 2026-09-02, inLanguage es-CR) por página + `og:image:alt` + `<link rel="manifest">` + registro de service worker (is:inline, sin condicionar por el host de desarrollo local, para cumplir verify-build); ③ PWA nueva: `public/manifest.webmanifest` (start_url /, theme #9f3428, icons 192/512 PNG generados con System.Drawing desde apple-touch-icon.png + maskable + svg) y `public/sw.js` (network-first propio origen, precache páginas, sin interceptar terceros); ④ `index.astro` TouristAttraction: `alternateName` pasa a array ["Mercado Central","San José Central Market"] + campo `hasMap` (maps.app.goo.gl/DFX6n41sMX3djrEA6); ⑤ iframe del mapa sustituido por el embed pb exacto facilitado (UI zh-CN, mismo POI) y añadido enlace de fuente oficial (Municipalidad de San José + Visit Costa Rica/ICT) bajo la columna de mapa. Confirmado contra los datos facilitados: dominio sanjosecentralmarket.com, Plus Code WWM9+R59, teléfono +506 2222 5981, rating 4,4 (31.375), horario de ficha 09:00–18:00 ya reflejados (nota de prioridad municipal conservada). read_lints 0 errores; `astro build` + verify-build exit 0 (6 HTML, sitemap 5 URL, sin contenido prohibido); verificación de artefactos OK (canonical/og/org/webpage/manifest/sw/iconos/embed todos presentes). Pendiente: `pnpm deploy` en el entorno con acceso a Cloudflare.
- Auditoría de cumplimiento (2026-09-02, lista regenerada a partir de los ítems conocidos): verificación ítem por ítem con evidencia de código. Aprobado: ① JSON-LD TouristAttraction (BaseLayout no lo emite; `index.astro` L14–29: @type array TouristAttraction+LocalBusiness, @id absoluto `...#mercado-central-san-jose`, alternateName array, address/geo/hasMap/telephone/priceRange/openingHoursSpecification/aggregateRating 4,4–31.375/isAccessibleForFree/sameAs 4 fuentes) + FAQPage 8 preguntas (L40/L44) + @graph WebSite/Organization/WebPage en cada página (BaseLayout L9–17, WebPage con dateModified 2026-09-02). ② Horarios: prioridad municipal L–S 07:30–19:00 y domingo 08:00–18:00 en facts y schema, con nota pública de la diferencia frente a la ficha de Google 09:00–18:00 (L163). ③ E-E-A-T: pies de página con declaración independiente/sin fines de lucro, fuentes contrastadas y aviso de derechos de imagen; páginas legales con "Última actualización: septiembre de 2026". ④ SITE='https://sanjosecentralmarket.com' → canonical/og absolutos; sitemap-index.xml 5 URL verificado; robots.txt declara Sitemap. ⑤ Fotografías: 6 JPG reales locales + PHOTO-SOURCES.md + README-REAL-PHOTOS.txt + página de créditos con autor/CC por archivo; sin hotlinks (verify-build OK). ⑥ GA4 G-HXM22WWPKP sólo con consentimiento (BaseLayout L70–81 carga bajo `mc-sj-cookie-preferences`.analytics; /configuracion-de-cookies/ guarda y dispara `__mcLoadGA`). ⑦ PWA: manifest.webmanifest + sw.js (network-first, mismo origen) + registro + icons 192/512 + theme-color. ⑧ 404: página propia + wrangler `not_found_handling: "404-page"`. ⑨ Enlaces oficiales: Municipalidad/ICT/Wikidata/IMN/Open-Meteo sin texto prohibido (audit-source exit 0). ⑩ Idioma: español es-CR sin restos del template (audit-language exit 0). Defectos hallados y corregidos: (a) la propia nota de validación de la ronda PWA contenía la cadena textual que dispara `scripts/audit-source.mjs` → regresión exit 1 en `pnpm audit:source`; reescrita sin esa cadena, ahora exit 0; (b) la línea "Pendiente: definir el dominio real" quedó superada y se marcó como tal; (c) `PACKAGE-CONTENTS.txt` no listaba manifest/sw/iconos 192-512/WeatherSection → actualizado; (d) `astro check` (antes nunca ejecutado): 0 errores y 4 hints `astro(4000)` por JSON-LD sin `is:inline` → directiva añadida en BaseLayout (2) e index.astro (2); lint 0 errores. Notas no bloqueantes: sitemap sin `<lastmod>` (estadística de verify-build, 0 con lastmod; @astrojs/sitemap no lo emite por defecto); `mercado-central-san-jose-condimentos.jpg` registrado en créditos pero sin `<img>` en la página (recurso de reserva); `pnpm deploy` sigue pendiente en entorno con acceso a Cloudflare.
