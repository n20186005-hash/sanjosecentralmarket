import fs from 'node:fs';import path from 'node:path';const root='src';const forbidden=['隐私政策','服务条款','中央市场','常见问答'];let bad=[];function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(/\.(astro|html|css)$/.test(e.name)){const t=fs.readFileSync(p,'utf8');for(const x of forbidden)if(t.includes(x))bad.push(`${p}: ${x}`)}}}walk(root);if(bad.length){console.error(bad.join('\n'));process.exit(1)}console.log('Language audit OK');
